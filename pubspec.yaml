name: indirim_radari_admin
description: "İndirim Kapısı Admin Paneli"
publish_to: 'none'
version: 1.0.0+88

environment:
  sdk: ^3.6.2

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  firebase_core: ^3.6.0
  cloud_firestore: ^5.4.0
  firebase_auth: ^5.3.0
  firebase_messaging: ^15.1.0
  intl: any
  uuid: ^4.5.0
  http: ^1.2.0
  firebase_storage: ^12.0.0
  image_picker: ^1.1.2
  file_picker: ^8.0.0
  shared_preferences: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
